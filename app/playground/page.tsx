import { PlaygroundClient } from '@/components/pages/PlaygroundClient';

export const metadata = { title: 'Python playground' };

export default function PlaygroundPage() {
  return <PlaygroundClient />;
}
